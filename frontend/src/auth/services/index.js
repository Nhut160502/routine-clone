import {
  index as getListGroupProduct,
  store as storeGroupProduct,
  show as showGroupProduct,
  update as updateGroupProduct,
  destroy as destroyGroupProduct,
} from "./groupProducts";

import {
  index as getListCollection,
  store as storeCollection,
  show as showCollection,
  update as updateCollection,
  destroy as destroyCollection,
} from "./collections";

import {
  index as getListCategory,
  store as storeCategory,
  show as showCategory,
  update as updateCategory,
  destroy as destroyCategory,
} from "./categories";

import {
  index as getListCategoryChild,
  store as storeCategoryChild,
  show as showCategoryChild,
  update as updateCategoryChild,
  destroy as destroyCategoryChild,
} from "./categoryChilds";

import {
  index as getListColor,
  store as storeColor,
  show as showColor,
  update as updateColor,
  destroy as destroyColor,
} from "./colors";

import {
  index as getListSize,
  store as storeSize,
  show as showSize,
  update as updateSize,
  destroy as destroySize,
} from "./sizes";

import {
  index as getListDesign,
  store as storeDesign,
  show as showDesign,
  update as updateDesign,
  destroy as destroyDesign,
} from "./designs";

import {
  index as getListMaterial,
  store as storeMaterial,
  show as showMaterial,
  update as updateMaterial,
  destroy as destroyMaterial,
} from "./materials";

import {
  index as getListSex,
  store as storeSex,
  show as showSex,
  update as updateSex,
  destroy as destroySex,
} from "./sex";

import {
  index as getListHandType,
  store as storeHandType,
  show as showHandType,
  update as updateHandType,
  destroy as destroyHandType,
} from "./handTypes";

import {
  index as getListForm,
  store as storeForm,
  show as showForm,
  update as updateForm,
  destroy as destroyForm,
} from "./forms";

export {
  // groupProduct
  getListGroupProduct,
  storeGroupProduct,
  showGroupProduct,
  updateGroupProduct,
  destroyGroupProduct,

  // Collection
  getListCollection,
  storeCollection,
  showCollection,
  updateCollection,
  destroyCollection,

  // Caetgory
  getListCategory,
  storeCategory,
  showCategory,
  updateCategory,
  destroyCategory,

  // CategoryChild
  getListCategoryChild,
  storeCategoryChild,
  showCategoryChild,
  updateCategoryChild,
  destroyCategoryChild,

  // Colors
  getListColor,
  storeColor,
  showColor,
  updateColor,
  destroyColor,

  // Sizes
  getListSize,
  storeSize,
  showSize,
  updateSize,
  destroySize,

  // Design
  getListDesign,
  storeDesign,
  showDesign,
  updateDesign,
  destroyDesign,
  getListMaterial,
  storeMaterial,
  showMaterial,
  updateMaterial,
  destroyMaterial,

  // Sex
  getListSex,
  storeSex,
  showSex,
  updateSex,
  destroySex,

  // Hand Type
  getListHandType,
  storeHandType,
  showHandType,
  updateHandType,
  destroyHandType,

  // Form
  getListForm,
  storeForm,
  showForm,
  updateForm,
  destroyForm,
};
