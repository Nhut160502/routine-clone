import {
  index as getListCollection,
  show as showCollection,
} from "./collections";

import { findByCollection, showProduct } from "./product";

export { getListCollection, showCollection, findByCollection, showProduct };
